clone_depth: 50

image:
  - Ubuntu

install:
  - ps: Install-Product node $env:nodejs_version

before_test:
  - npm install

test_script:
  - node --version
  - npm --version

cache:
  - node_modules

os:
  - Ubuntu 16.04.6 LTS (Xenial Xerus)

build: off

matrix:
  fast_finish: true
